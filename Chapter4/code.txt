

flask run

flask run --host=0.0.0.0 --port=8080

FLASK_APP=app.py
FLASK_ENV=development
production
testing
FLASK_DEBUG=1 개발모드 
FLASK_DEBUG=0 프로덕션모드

import logging

logging.getLogger('werkzeug').setLevel(logging.ERROR)


운영환경 gunicorn -w 4 -b 127.0.0.1:5000 ex01:app

